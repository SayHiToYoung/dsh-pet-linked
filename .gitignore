# Python
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store

# ---- 二次开发新增的本地运行时产物（不随仓库发布）----
# 本地虚拟环境 / 运行日志 / pid / 调试日志
.venv/
.run/
# 信标注入备份
.beacon-backup/
# 下载的发布包
releases/
# 访达 App（用 scripts/make-app.sh 重新生成）
联动桌宠.app/

# 运行时产物
tmp_appdata/
tests/_tmp_*/

# 素材：只提交 assets/characters（角色动画），其他 assets 内容不入库
assets/*
!assets/characters/
!assets/characters/**
assets/thumb/
assets/videos/
assets/animations/
assets/prompt/

# 点击音效（内置合成 click.wav，随包分发；用户可自定义覆盖）
!assets/sounds/
!assets/sounds/**

# 彩蛋弹窗图片池（fun_image_popup 随机取图，随包分发）
!assets/big_blue_fat_fish/
!assets/big_blue_fat_fish/**

# Windows 安装包图标（本地生成，入库保证克隆后可构建）
!assets/icon.ico
# 鲸鱼 Dock 图标（二次开发：源码运行时的应用/Dock 图标，入库保证克隆后可运行）
!assets/icon.icns

# PyInstaller 构建产物
build/
dist/
*.spec

# 本地开发文档（不随仓库发布）
LOG.md
LOG-INDEX.md
SPEC.md
# onedir build artifacts
dist-onedir/
build-onedir/

# 构建注入的变体标识（由 scripts/build_onedir.ps1 生成）
packaging/build_variant.py

# 本地实验目录（含嵌套 git 仓库，不入库）
.research/

# 聊天背景主题壁纸（聊天背景功能随包分发）
!assets/chat/
!assets/chat/**

# 全局忽略 macOS 元数据（置于所有白名单之后，防止被 !** 反向放行）
**/.DS_Store

# —— 活动记忆的运行时产物:绝不进仓库 ——
# 方向文档第 5 节:「行为日志按敏感数据对待」。activity_memory 会写下前台应用、
# 窗口标题与时长,memory_server 会落 SQLite —— 这是「你一整天在电脑上干了什么」。
# 现在这些数据碰巧落在仓库外,但那是运气不是拦截:一次 `git add -A` 就够把它写进
# 公开仓库的历史,而 git 历史里的这类东西最难擦干净。规则要写在这里,不能只写在文档里。
*.db
*.sqlite
*.sqlite3
*.jsonl
memory_store/
.memory/
